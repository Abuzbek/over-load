# Copy to .env. Which app profile to build: development | preview | production.
# Picks the bundle id, app name and Firebase config folder (see apps/mobile/app.config.js).
EXPO_PUBLIC_APP_ENV=development
