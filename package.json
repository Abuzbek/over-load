{
  "name": "overload",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "start": "pnpm --filter @overload/mobile start",
    "ios": "pnpm --filter @overload/mobile ios",
    "android": "pnpm --filter @overload/mobile android",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "bundle": "pnpm --filter @overload/mobile exec expo export --platform ios",
    "seed:build": "pnpm --filter @overload/seed-exercises build",
    "db:generate": "pnpm --filter @overload/schema generate"
  },
  "devDependencies": {
    "react-native-web": "^0.21.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
